import java.io.File;
import java.util.ArrayList;

public class Loader {

	private ArrayList<File> userFiles;
	private ArrayList<File> artworkList;
	private static ArrayList<User> users;
	private static ArrayList<Artwork> artworks;

	public static void readUserFiles() {

	}

	
	
	
	/*
	 * 
	 * + readUserFile( filename : String) : void 
	 * + getSelectedUsers(name: String ): ArrayList<User> 
	 * + getSelectedArtworks(name: String) ArrayList<Artwork>
	 * + getNewUsers() : ArrayList<User> 
	 * + getNewArtworks() : ArrayList<Artwork> 
	 * + getFavouriteArtworks(username: String) : ArrayList<Artwork> 
	 * +getFavouriteUsers(username: String) : ArrayList<User> 
	 * - readDataFromUserFile( in : Scanner) : ArrayList<User> 
	 * + readArtworkFile( filename : String) : void
	 * - readDataFromArtworkFile( in : Scanner) : ArrayList<Artwork> 
	 * + readBidFile(filename : String) : void 
	 * - readFromBidFile(in : Scanner) : ArrayList<Bid> 
	 * + getUserBidHistory(filename : String, username : String) : ArrayList<Bid>
	 * 
	 * 
	 */

}
